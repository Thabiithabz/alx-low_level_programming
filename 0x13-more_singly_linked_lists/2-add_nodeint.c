#include "lists.h"

/**
 * add-nodeint - add node at the beginning of list
 * @head: pointer to head pointer which points first node
 * @n: new int created
 * Return: the address of the new element, or NULL if it failed
 */
listint_t *add_nodeint(listint_t **head, const int n)
{
	listint_t *new_node;

	if (head == NULL)
		return (0);

	new_node = malloc(sizeof(listint_t);
			if (new_node == NULL)
			return (NULL);
			if (*head == NULL)
			new_node->next = NULL;
			else
			new_node->h = h;
			*head = new_node;

			return (0);
}
